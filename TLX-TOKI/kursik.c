#include <stdio.h>

int main(){
    int N, M, K, h=0, i=2, j, h, c=0;

    scanf("%d %d %d", &N, &M, &K);
    int a[N][M];
    if(k==N*M){
        printf("%d %d", N, M);
    }
    while(i<M+N){
        h=i-1;
        for(j=1; j<i; j++){
            if(h==0){
                break;
            }
            if()
        }
    }
    
    return 0;
}