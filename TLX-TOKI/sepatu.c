#include <stdio.h>
#include <stdlib.h>

int main(){
    int N, M, i, h=0, p=0;

    scanf("%d %d", &N, &M);
    int k[N], l[M];
    for(i=0; i<N; i++){
        scanf("%d", k[i]);
    }
    qsort()
    return 0;
}