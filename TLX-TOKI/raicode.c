#include <stdio.h>

int main(){
    int a, b, h;

    scanf("%d %d %d", &a, &b, &c);
    if(a<=60 || b<=25){
        printf("%d %d", a, a*25);
    }else if{

    }else{
        printf("tidak sesuai");
        printf("%d %d", a, b);
    }
    return 0;
}